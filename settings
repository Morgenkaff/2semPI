# Uncomment lines with '#'

# Set motor type
# 1 for stepper, 0 for dc
type = 1

# Set motor speed
# 1 fast, 2 medium, 3 slow
speed = 1

# Set to 1 if system should go to halt after exiting program
halt = 0
